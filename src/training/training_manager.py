import pandas as pd

from src.model.model_config import ModelConfig
from src.training.training_config import TrainingConfig


def run_training(training_config: TrainingConfig,
                 model_config: ModelConfig,
                 metadata: pd.DataFrame):
    pass
